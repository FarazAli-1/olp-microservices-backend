server:
  port: 8084

spring:
  application:
    name: notification-service
  data:
    redis:
      host: redis
      port: 6379

eureka:
  client:
    service-url:
      defaultZone: http://service-discovery:8761/eureka